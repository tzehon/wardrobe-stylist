#include "Config.xcconfig"

// Optional local development values. Missing values keep unit tests and the local-first
// app shell buildable; connected features fail closed until configured.
#include? "Secrets.xcconfig"
