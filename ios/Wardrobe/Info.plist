<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundleDisplayName</key>
    <string>Wardrobe Stylist</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UILaunchScreen</key>
    <dict>
        <key>UIColorName</key>
        <string>LaunchBackground</string>
        <key>UIImageName</key>
        <string>LaunchMark</string>
        <key>UIImageRespectsSafeAreaInsets</key>
        <true/>
    </dict>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>GIDClientID</key>
    <string>$(GOOGLE_CLIENT_ID)</string>
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleTypeRole</key>
            <string>Editor</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>$(GOOGLE_REVERSED_CLIENT_ID)</string>
            </array>
        </dict>
    </array>
    <key>BackendBaseURL</key>
    <string>$(BACKEND_SCHEME)://$(BACKEND_HOST)</string>
    <key>BackendDeviceToken</key>
    <string>$(BACKEND_DEVICE_TOKEN)</string>
    <key>PRIVACY_POLICY_URL</key>
    <string>$(PRIVACY_POLICY_URL)</string>
    <key>SUPPORT_URL</key>
    <string>$(SUPPORT_URL)</string>
    <!-- Export compliance: the app uses only standard HTTPS/TLS (Fly.io backend,
         Google/Gmail, Anthropic), which qualifies for the encryption exemption.
         Declaring NO here spares an export-compliance prompt on every TestFlight
         upload. Revisit if we ever add non-exempt/custom cryptography. -->
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>
    <!-- Phase 4: in-app camera capture for manually adding wardrobe items.
         Library picking uses PhotosPicker (out-of-process, no key required). -->
    <key>NSCameraUsageDescription</key>
    <string>Wardrobe uses the camera so you can photograph clothing to add it to your wardrobe catalog.</string>
    <!-- Phase 6: opportunistic background receipt sync. `processing` covers the
         BGProcessingTask that reruns ReceiptPipeline.sync roughly daily. The
         permitted-identifiers list must include every task id we register/submit,
         or BGTaskScheduler throws at runtime. -->
    <key>UIBackgroundModes</key>
    <array>
        <string>processing</string>
    </array>
    <key>BGTaskSchedulerPermittedIdentifiers</key>
    <array>
        <string>com.tth.Wardrobe.receiptSync</string>
    </array>
</dict>
</plist>
