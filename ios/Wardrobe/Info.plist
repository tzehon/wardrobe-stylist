<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UILaunchScreen</key>
    <dict/>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>GIDClientID</key>
    <string>$(GOOGLE_CLIENT_ID)</string>
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleTypeRole</key>
            <string>Editor</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>$(GOOGLE_REVERSED_CLIENT_ID)</string>
            </array>
        </dict>
    </array>
    <key>BackendBaseURL</key>
    <string>$(BACKEND_SCHEME)://$(BACKEND_HOST)</string>
    <key>BackendDeviceToken</key>
    <string>$(BACKEND_DEVICE_TOKEN)</string>
    <key>NSAppTransportSecurity</key>
    <dict>
        <!-- DEV-ONLY: allow plain HTTP so the iPhone can reach the Mac dev backend
             at its private LAN IP. NSAllowsLocalNetworking on its own covers .local
             Bonjour names and link-local (169.254.x.x) but NOT private network ranges
             (192.168/16 etc.), which is what we actually need here. Turn this off once
             the backend is on Fly.io behind HTTPS. -->
        <key>NSAllowsArbitraryLoads</key>
        <true/>
    </dict>
    <!-- iOS 14+ requires apps to declare why they reach private-network IPs and
         prompts the user on first attempt. Without this key the request fails
         silently with "the internet connection appears to be offline". -->
    <key>NSLocalNetworkUsageDescription</key>
    <string>Wardrobe talks to its development backend running on your Mac to extract purchase receipts. Production builds use a remote server.</string>
</dict>
</plist>
